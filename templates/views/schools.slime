h1 Schools
div class="gridcontainer"
    = for grej <- Pluggy.School.get_all() do
        div class="box"
            h1 class="hero" = grej
            section class="options"
                a EDIT
                a REMOVE